spring:
  application:
    name: mos_erp_product_module
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}

eureka:
  instance:
    nonSecurePort: ${server.port:9303}
    preferIpAddress: true
    statusPageUrlPath: /structure
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URL:http://localhost:8761}/eureka/
