spring:
  application:
    name: config-server

eureka:
  instance:
    nonSecurePort: ${server.port:8888}
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_URL:localhost:8761}/eureka/
